# 计算机网络 10 题
SEED = [
  {
    "subject": "计算机网络", "question_type": "分析题",
    "question": r"简述 OSI 七层模型与 TCP/IP 四层模型的对应关系，并说明各层的核心功能。",
    "answer": r"OSI：物理层、数据链路层、网络层、传输层、会话层、表示层、应用层。TCP/IP：网络接口层、网际层(IP)、传输层(TCP/UDP)、应用层。对应：OSI 上三层 → TCP/IP 应用层；OSI 下两层 → 网络接口层。",
    "key_breakthrough": r"抓两个模型的「分合」：TCP/IP 把 OSI 的会话+表示+应用三层合并为应用层，把物理+数据链路合并为网络接口层。记各层功能时记住关键词——网络层「找路（IP/路由）」、传输层「端到端可靠（TCP/端口）」、应用层「协议（HTTP/DNS）」。",
    "knowledge_points": ["OSI模型", "TCP/IP模型", "分层", "封装"],
    "knowledge_review": r"分层的核心思想是「封装与解耦」：每层为上层提供服务，使用下层的服务，层间通过接口交互。数据发送时自上而下封装（应用数据 → 段 → 数据报 → 帧 → 比特），接收时自下而上解封装。OSI 是理论模型（七层，国际标准），TCP/IP 是实际使用的工业标准（四层）。各层典型协议：网络层 IP/ICMP/路由协议；传输层 TCP/UDP；应用层 HTTP/DNS/SMTP/FTP。易错点：记住「交换机工作在数据链路层（按 MAC 转发）、路由器工作在网络层（按 IP 转发）」。",
    "extensions": [r"说明数据从发送方应用层到接收方应用层的封装/解封装过程。", r"路由器、交换机、集线器分别工作在哪些层？"],
  },
  {
    "subject": "计算机网络", "question_type": "计算题",
    "question": r"某公司申请到 C 类地址 202.120.10.0，需划分 4 个子网，求子网掩码及每个子网的网络地址与可用主机范围。",
    "answer": r"子网掩码 255.255.255.192（/26）；4 个子网：202.120.10.0/26、.64/26、.128/26、.192/26，每个子网 62 个可用主机。",
    "key_breakthrough": r"划 4 个子网需借 2 位主机位（$2^2=4$），掩码从 /24 变 /26（255.255.255.192），每子网 $2^6-2=62$ 台主机（减网络地址和广播地址）。块大小 = $256-192=64$，所以网络地址按 64 递增。",
    "knowledge_points": ["子网划分", "子网掩码", "CIDR", "网络地址"],
    "knowledge_review": r"子网划分：从主机位借位作为子网位。借 n 位得到 $2^n$ 个子网，每子网主机数为 $2^{(主机位数)}-2$（减网络地址和广播地址）。C 类默认 /24（255.255.255.0），借 2 位 → /26（255.255.255.192）。关键计算：块大小 = 256 - 掩码末字节（256-192=64），网络地址是块大小的整数倍。可用主机范围 = 网络地址+1 ~ 广播地址-1。易错点：① 忘记减 2（网络地址和广播地址不可用）；② 混淆「子网数」与「可用子网数」（现代 CIDR 允许全 0/全 1 子网）。",
    "extensions": [r"若需 6 个子网且每子网不少于 30 台主机，如何划分？", r"给出 IP 202.120.10.130/26，判断它属于哪个子网，广播地址是多少？"],
  },
  {
    "subject": "计算机网络", "question_type": "分析题",
    "question": r"详细描述 TCP 三次握手的过程，并说明为什么需要三次而不是两次。",
    "answer": r"① 客户端发 SYN=1, seq=x；② 服务器回 SYN=1, ACK=1, seq=y, ack=x+1；③ 客户端发 ACK=1, seq=x+1, ack=y+1。三次是为了**确认双方收发能力都正常**并**防止已失效的连接请求报文突然到达服务器造成错误**。",
    "key_breakthrough": r"两次握手只能确认「客户端→服务器」方向畅通，无法确认「服务器→客户端」方向是否可用。第三次握手正是客户端对服务器序号的确认——**本质是双向确认，缺一不可**。经典反例：客户端的旧 SYN 延迟到达，服务器若两次握手就建立连接会白白占用资源。",
    "knowledge_points": ["TCP", "三次握手", "连接建立", "SYN"],
    "knowledge_review": r"TCP 是面向连接的可靠传输。三次握手同时完成两件事：① 双方交换初始序列号（ISN），为后续可靠传输和按序重组打基础；② 确认双方的发送和接收能力均正常。状态变迁：客户端 CLOSED → SYN_SENT → ESTABLISHED；服务器 LISTEN → SYN_RCVD → ESTABLISHED。若第三次握手丢失，服务器超时重传 SYN+ACK。SYN 洪泛攻击正是利用服务器在 SYN_RCVD 状态会分配资源这一点（防护：SYN Cookie）。易错点：三次握手交换的是「序列号」而非数据；握手后才能真正传数据。",
    "extensions": [r"画出三次握手的状态变迁图与报文段字段。", r"什么是 SYN 洪泛攻击？SYN Cookie 如何应对？"],
  },
  {
    "subject": "计算机网络", "question_type": "分析题",
    "question": r"说明 TCP 四次挥手的过程，以及为什么需要 TIME_WAIT 状态（等待 2MSL）。",
    "answer": r"① 主动方发 FIN；② 被动方回 ACK（此时被动方可能还有数据要发）；③ 被动方数据发完后发 FIN；④ 主动方回 ACK 并进入 TIME_WAIT，等待 2MSL 后关闭。",
    "key_breakthrough": r"挥手比握手多一次，是因为**TCP 是全双工的，两个方向要分别关闭**：被动方收到 FIN 时可能还有数据未发完，所以 ACK 和 FIN 不能合并成一次发送（除非恰好同时）。TIME_WAIT 的两个作用：① 保证最后的 ACK 能到达对方（丢失则对方重发 FIN）；② 让本次连接的残留报文在网络中消散，避免干扰新连接。",
    "knowledge_points": ["TCP", "四次挥手", "TIME_WAIT", "MSL"],
    "knowledge_review": r"TCP 连接是全双工，关闭需双方各自发送 FIN 并收到确认。TIME_WAIT 出现在主动关闭方，持续 2MSL（MSL 最大报文段生存时间，通常 2 分钟）。两个作用：① 可靠地终止连接——若最后的 ACK 丢失，被动方会重发 FIN，主动方能再次响应；② 防止旧连接中延迟的重复报文段被之后复用同一四元组的新连接错误接收。大量 TIME_WAIT 会占用端口资源（高并发短连接服务器上的常见问题，可用 SO_REUSEADDR 缓解）。易错点：TIME_WAIT 在主动关闭方而非被动方；2MSL 而非 1MSL。",
    "extensions": [r"画出四次挥手的状态变迁图。", r"服务器上出现大量 TIME_WAIT 是什么原因？如何处理？"],
  },
  {
    "subject": "计算机网络", "question_type": "分析题",
    "question": r"比较 TCP 与 UDP 的区别，并说明各自适用的应用场景。",
    "answer": r"TCP：面向连接、可靠（确认/重传/排序/流量控制/拥塞控制）、有拥塞控制、首部 20 字节、面向字节流；UDP：无连接、不可靠、尽最大努力交付、首部 8 字节、面向报文、支持一对多。",
    "key_breakthrough": r"核心权衡就是**可靠性 vs 实时性/开销**。要「一个都不能错」用 TCP（文件、网页）；要「快、丢几帧无所谓」用 UDP（音视频、实时游戏、DNS 查询）。",
    "knowledge_points": ["TCP", "UDP", "可靠性", "应用场景"],
    "knowledge_review": r"TCP 提供可靠、有序、不重复的字节流服务，通过序列号/确认/重传/滑动窗口/拥塞控制实现，首部至少 20 字节，有连接建立与释放开销。UDP 仅在 IP 之上加了复用/分用和差错检测，首部仅 8 字节，无连接、不保证可靠与顺序，但延迟低、开销小、支持广播/多播。应用：TCP — HTTP/HTTPS、FTP、SMTP、SSH、数据库连接；UDP — DNS、DHCP、SNMP、音视频通话（RTP 常跑在 UDP 上）、在线游戏、QUIC（HTTP/3 基于 UDP 自建可靠层）。易错点：UDP 并非「完全不可靠」，应用层可自行实现可靠；UDP 校验和可选（IPv4 中可置 0）。",
    "extensions": [r"为什么 DNS 用 UDP 而不是 TCP？（提示：查询短、要求快）", r"HTTP/3 为什么改用基于 UDP 的 QUIC？"],
  },
  {
    "subject": "计算机网络", "question_type": "计算题",
    "question": r"设信道带宽为 1 Mbps，往返时延 RTT = 20 ms，TCP 的发送窗口为多少字节时才能充分利用信道？（忽略传输与处理时延）",
    "answer": r"窗口至少 = 带宽 × RTT = $10^6 \text{ bps} \times 0.02 \text{ s} = 20000 \text{ bit} = 2500 \text{ 字节}$",
    "key_breakthrough": r"这是「带宽时延积（BDP）」的经典计算：要让管道填满，在途未确认的数据量必须 ≥ 带宽×RTT。若窗口小于 BDP，发送方会在等 ACK 时空闲，信道利用率不足。",
    "knowledge_points": ["带宽时延积", "滑动窗口", "信道利用率", "RTT"],
    "knowledge_review": r"带宽时延积 BDP = 带宽 × RTT，表示「管道容量」（链路上能容纳的最大未确认数据量）。TCP 的发送窗口必须 ≥ BDP 才能跑满带宽，否则利用率 = 窗口 / BDP。这也是为什么长肥网络（高带宽、高延迟，如卫星链路）需要更大的窗口——因此有了窗口扩大选项（Window Scale）。相关公式：信道利用率 = $\frac{L/C}{L/C + \text{RTT}}$（L 为窗口内数据量，C 为带宽）。易错点：单位换算（bit vs Byte，Mbps = 10^6 bps），1 字节 = 8 bit。",
    "extensions": [r"若窗口只有 1000 字节，信道利用率是多少？", r"什么是「长肥管道」？为什么需要窗口扩大选项？"],
  },
  {
    "subject": "计算机网络", "question_type": "分析题",
    "question": r"简述 TCP 的拥塞控制四种算法（慢开始、拥塞避免、快重传、快恢复）的工作过程。",
    "answer": r"慢开始：cwnd 从 1 个 MSS 起指数增长，达 ssthresh 转拥塞避免；拥塞避免：cwnd 线性增长（每 RTT +1 MSS）；检测到丢失（超时）则 ssthresh 减半、cwnd 回 1 重新慢开始；快重传：收到 3 个重复 ACK 立即重传而不等超时；快恢复：快重传后 ssthresh 减半、cwnd 设为 ssthresh（而非回 1），直接进拥塞避免。",
    "key_breakthrough": r"把四种算法理解成一个「探测—加速—刹车」的闭环：慢开始和拥塞避免是「探路」（指数→线性），超时是「急刹」（回 1），快重传/快恢复是「点刹」（减半后继续跑，不用重来）。**关键区分：超时 → 回 1（慢开始）；3 个重复 ACK → 减半（快恢复）。**",
    "knowledge_points": ["拥塞控制", "慢开始", "拥塞避免", "快重传", "快恢复"],
    "knowledge_review": r"TCP 拥塞控制维护 cwnd（拥塞窗口）和 ssthresh（慢开始门限），发送窗口 = min(cwnd, 接收窗口)。慢开始：cwnd 每收到一个 ACK 就 +1，因此每 RTT 翻倍（指数增长）。拥塞避免：每 RTT 只 +1（线性，加法增大）。丢包判断：① 超时 → 认为严重拥塞，ssthresh = cwnd/2，cwnd = 1，重新慢开始；② 收到 3 个重复 ACK → 认为轻度，触发快重传（立即重传丢失报文）+ 快恢复（ssthresh = cwnd/2，cwnd = ssthresh，直接线性增长）。整体是 AIMD（加性增、乘性减）。易错点：cwnd 的单位是 MSS（最大报文段长度），常换算成字节；慢开始并非真的「慢」（是指数增长）。",
    "extensions": [r"画出拥塞窗口随时间的典型变化曲线（含超时与快恢复两种事件）。", r"收到 3 个重复 ACK 就一定说明丢包吗？为何选 3 这个数？"],
  },
  {
    "subject": "计算机网络", "question_type": "分析题",
    "question": r"描述 DNS 的解析过程（以访问 www.example.com 为例），并说明递归查询与迭代查询的区别。",
    "answer": r"主机 → 本地 DNS 服务器（递归）→ 根域名服务器 → .com 顶级域服务器 → example.com 权威服务器（迭代），逐级拿到下一级服务器地址，最终获得 IP 返回给主机。",
    "key_breakthrough": r"**主机与本地 DNS 之间是递归（你问我，我给你最终答案），本地 DNS 与其他服务器之间是迭代（你问我，我给你线索，你自己去问下一级）**。记住这个分工，整个流程就不会乱。",
    "knowledge_points": ["DNS", "域名解析", "递归查询", "迭代查询"],
    "knowledge_review": r"DNS 把域名映射为 IP，采用分布式层次数据库（根 → 顶级域 → 权威域）。查询过程：浏览器缓存 → 操作系统缓存/hosts → 本地 DNS 服务器 → 根 → 顶级域 → 权威服务器，结果逐级缓存（有 TTL）。递归查询：请求者只发一次请求，由被询服务器负责查到最终结果（主机→本地 DNS 通常是递归）。迭代查询：被询服务器返回「下一步该问谁」，请求者自己继续查（本地 DNS→各级服务器通常是迭代）。记录类型：A（IPv4）、AAAA（IPv6）、CNAME（别名）、MX（邮件）、NS（域名服务器）。DNS 主要用 UDP 53 端口（报文小、快），区域传送用 TCP。易错点：DNS 缓存会导致修改记录后不能立即生效（受 TTL 影响）。",
    "extensions": [r"说明 DNS 缓存的作用及 TTL 的含义。", r"什么是 DNS 劫持？如何防范？"],
  },
  {
    "subject": "计算机网络", "question_type": "分析题",
    "question": r"说明 HTTP 与 HTTPS 的区别，并简述 HTTPS 的握手过程（TLS）。",
    "answer": r"HTTP 明文传输（80 端口），HTTPS = HTTP + TLS 加密（443 端口），提供机密性、完整性和身份认证。TLS 握手：客户端发支持的加密套件 → 服务器回送证书 → 客户端验证证书并生成预主密钥（用服务器公钥加密）→ 双方派生会话密钥 → 后续对称加密通信。",
    "key_breakthrough": r"HTTPS 的精髓是 **「非对称加密交换密钥 + 对称加密传数据」**：非对称（RSA/ECDHE）只用在握手阶段协商出一个会话密钥，真正传数据用对称加密（AES），兼顾安全与性能。证书解决的是「你连的到底是不是真的服务器」这个身份问题。",
    "knowledge_points": ["HTTP", "HTTPS", "TLS", "证书", "对称与非对称加密"],
    "knowledge_review": r"HTTP 是无状态的明文应用层协议，HTTPS 在 HTTP 与 TCP 之间加入 TLS/SSL 层。TLS 握手核心：① 协商加密套件；② 服务器出示数字证书（含公钥，由 CA 签名）；③ 客户端验证证书链是否可信；④ 用非对称加密安全交换「预主密钥」；⑤ 双方独立派生出相同的会话密钥；⑥ 后续用对称加密（如 AES-GCM）传输，并带 MAC 保证完整性。为什么混合使用：非对称加密安全但慢，对称加密快但密钥分发难——用非对称传密钥、对称传数据，各取所长。易错点：HTTPS 并非绝对不可破解，安全性依赖证书体系与加密套件强度；中间人攻击主要通过伪造证书实现。",
    "extensions": [r"说明数字证书的作用及 CA 的工作机制。", r"HTTPS 一定比 HTTP 慢吗？慢在哪、如何优化？"],
  },
  {
    "subject": "计算机网络", "question_type": "计算题",
    "question": r"设以太网采用 CSMA/CD，端到端往返时延为 $2\tau = 51.2\,\mu s$，数据传输速率为 10 Mbps。求最短帧长（字节）。",
    "answer": r"最短帧长 = $10 \times 10^6 \text{ bps} \times 51.2 \times 10^{-6} \text{ s} = 512 \text{ bit} = 64 \text{ 字节}$",
    "key_breakthrough": r"CSMA/CD 要求「**发完之前必须能检测到冲突**」，所以帧的发送时间必须 ≥ 端到端往返时延 2τ。于是最短帧长 = 发送速率 × 2τ = 64 字节（这就是以太网最小帧长 64 字节的由来）。",
    "knowledge_points": ["CSMA/CD", "最短帧长", "冲突检测", "以太网"],
    "knowledge_review": r"CSMA/CD（载波监听多路访问/冲突检测）是以太网的核心介质访问控制方法，流程：载波监听（信道空闲才发）→ 边发边检测冲突 → 冲突则停发并发送阻塞信号 → 二进制指数退避后重发。关键点：发送站必须在发完帧之前能知道是否发生冲突，因此帧发送时延 ≥ 端到端往返传播时延 2τ（最坏情况：信号刚到对端时对方开始发，冲突信号需 τ 传回）。10 Mbps × 51.2 μs = 512 bit = 64 字节，即经典以太网最短帧长。若帧过短（如小于 64 字节），需填充（pad）。易错点：用的是往返时延 2τ 而非单程 τ；单位换算（bit 与字节）。",
    "extensions": [r"若速率升到 100 Mbps 且保持 64 字节最短帧，允许的 2τ 是多少？（网络直径会缩小）", r"二进制指数退避算法如何工作？"],
  },
]
