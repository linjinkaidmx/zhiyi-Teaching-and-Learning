# P9 人工点检清单

> 线上地址：http://193.112.28.51:3300
> 自动已验证的部分见文末「已经验过、你不用重复」。
> 用法：逐项点一遍，不通过的记下「序号 + 现象」，发我即可。

---

## 一、必须你来做的（我技术上做不到）

### A. 真实账号全流程（headless 只能验弹窗渲染，验不了真实登录）

| # | 步骤 | 预期 |
|---|---|---|
| A1 | 「我的」→ 登录 / 注册 → 注册新账号 | 注册成功后弹出**备份码**，提示只显示一次 |
| A2 | 抄下备份码，关闭弹窗 | 顶部「我的」处显示昵称首字，页面变已登录态 |
| A3 | 退出登录 → 用「昵称 + 密码」重新登录 | 登录成功，**错题本与打卡记录自动拉回** |
| A4 | 「我的」→ 修改密码 → 用新密码退出重登 | 新密码可登录，旧密码失败 |
| A5 | 退出 → 登录页点「忘记密码」→ 用备份码重设 | 重设成功后自动登录 |
| A6 | 「我的」→ 改昵称（输入密码） | 提示"云端数据已迁移"，用**新昵称**能登录、数据仍在 |
| A7 | 换一台设备/浏览器登录同一账号 | 错题本、打卡、资料都在；对话与学习记录目前**只在本机**（预期行为） |
| A8 | 注销账号（输入「注销」确认） | 云端数据清空，回到本地模式 |

### B. 手机真机体验（我只能用无头浏览器模拟，模拟不出真机的相机/键盘/触控）

| # | 步骤 | 预期 |
|---|---|---|
| B1 | 手机浏览器打开首页 | 从上到下：欢迎语 → 配置引导 → **拍题卡** → 继续学习 → 今日复习 → AI 推荐 → 课表 |
| B2 | 底部五项导航逐个点 | 均可切换，当前项高亮；页面底部内容**不被导航遮住** |
| B3 | 拍题页点「拍照」 | 唤起系统相机；拍完直接回到拍题页并显示缩略图 |
| B4 | 拍题页点「上传图片」选相册图 | 正常选中并预览 |
| B5 | 讲解页底部输入框 | 手机键盘弹出时**不遮住输入框** |
| B6 | 讲解页长公式 | 公式不溢出、可横向滚动查看，不出现 `$` 符号 |
| B7 | 错题学习页筛选 | 筛选区在移动端是收起的，点「筛选」展开 |
| B8 | 课程表页 | 移动端为**按天列表**（不是网格），可加课/删课 |
| B9 | 「我的 → 偏好设置 → 界面主题」切深色 / 浅色 | 立即生效、刷新后保持；两种主题下文字都清晰可读，无"浅底浅字/深底深字" |
| B10 | 拍题页选图后**点缩略图** | 全屏打开大图，**双指捏合**能放大缩小、放大后能拖动、点遮罩退出 |
| B11 | 弹窗里故意留空 / 两次密码打不一样 | 错误提示显示在**输入框下方**（红字 + 红框），不需要去别处找 |

### C. 真实 AI 内容质量（接口是真实的，但我无法判断讲解对不对）

> 用的是你自己的 API Key（识别=方舟、讲解=DeepSeek R1、批改=DeepSeek chat），会计费。

| # | 步骤 | 检查点 |
|---|---|---|
| C1 | 拍 3 张不同科目的题（数学/专业基础课/含代码的题） | 识别文字是否完整、切题是否正确（该拆的拆了、不该拆的没拆） |
| C2 | 看讲解 | 思路是否讲清"为什么"，步骤有没有跳步；**有没有出现假公式/乱码** |
| C3 | 讲解页底部追问 2~3 轮 | 逐字出现、可停止；回答是否贴着这道题（不是泛泛而谈） |
| C4 | 讲解页「生成相似题 / 生成变式题」 | 目前是**演示数据**（后端未实现），文案是否有"演示"字样、不会让人误以为是真题 |
| C5 | 「更多 → 代码诊断」贴一段有 bug 的代码 | 是否能定位到具体行、给可运行的修正代码 |
| C6 | 「更多 → 算法演示」 | 动画能播、快慢档有效、步进滑块可用 |
| C7 | 「更多 → 考前速查」 | 搜索关键词能命中；公式显示正常；**打印本类 / 打印全部**预览是白底黑字 |
| C8 | AI 对话（/chat） | 新建对话 → 发送 → 逐字流式 → 停止 → 重新生成；「引用学习内容」只带勾选的条目 |

### D. 数据安全（不可逆操作，只有你能在真实数据上验证）

| # | 步骤 | 预期 |
|---|---|---|
| D1 | 「我的 → 导出全部数据」 | 下载 JSON，能打开、内容可读 |
| D2 | 「导入备份」选刚才的文件 → 合并 | 数据不重复、不丢失 |
| D3 | 再导入一次 → 覆盖 | 以备份为准，当前改动被替换（**先确保备份是最新的**） |
| D4 | 「以云端覆盖本地」（登录后） | 本地被云端替换；若换设备数据乱过，这一步应该能救回来 |
| D5 | 「清空本地数据」 | 本机清空、云端保留；**重新登录能拉回** |

### E. 观感拍板（我测了"可读性"，好不好看要你定）

- E1 深色主题：首页 / 讲解页 / 错题学习 / 我的 —— 卡片层级是否够分明、紫色是否过艳或过暗
- E2 浅色主题：同上
- E3 移动端：首页信息密度是否过高
- E4 有没有哪个页面"看起来像没做完"（空得发慌或挤成一团）

---

## 二、已经验过、你不用重复的

**自动回归（全绿）**
- 前端 8 套单元/契约测试：**317 项断言**（公式渲染 76 / 打卡 70 / 个人资料 46 / 路由 33 / 选课 26 / 课程 24 / 对话 23 / 首页 19）
- 后端 6 套接口测试：**95 项**（SSE / 云同步统计 14 / 账号 35 / LaTeX JSON 15 / SPA 深链回退 25 / **缓存头 6**）
- 路由冒烟：**17 个路由全部渲染成功**（含 404 兜底）

> **发新版后看不到更新？先强刷缓存**（Ctrl/Cmd+Shift+R，手机用无痕窗口）。
> 已修：`index.html` 原先没有 `Cache-Control`，浏览器会启发式缓存旧页面 → 已改为 `no-cache, must-revalidate`；
> 带哈希的 `/assets/*` 改为一年 `immutable`。可用 `curl -I http://193.112.28.51:3300/` 看到该头。

**P9 自动点检（26 项全通过，用 CDP 驱动真实 Chrome 实测）**
- 横向溢出：**3 档宽度（390 / 768 / 1440）× 20 条路由 = 60 组合，零溢出**
- 控制台错误：全部路由 **零错误**（含未捕获异常）
- 双主题：5 个页面 × 深浅两色，body 底色与主文字亮度**全部达标**（已修掉深色主题下 body 仍是浅色底的漏色）
- 交互：登录弹窗可打开 / 标题正确 / 注册表单 3 个输入框 / **Esc 可关闭**；拍题页文字输入校验生效；**界面主题开关可切换且立即生效**
- **弹窗内校验**：空表单点登录 → 出现**字段下方行内错误**「昵称长度需 2-20 位」+ 输入框变红（不再只靠 Toast）
- **提示层级与可读性**：Toast z-index **90 > 弹窗 70**（弹窗内提示不再被遮罩糊掉）、Toast 为**不透明实底**、位置在**页面中上方**
- **照片查看器**：选图后缩略图可点 → 全屏打开 → 图片 `contain` 完整不裁切 → 100% → 点「＋」变 150% → Esc 关闭

**性能与体积**
- 资源合计 **770KB**（移除 Element Plus 前 2037KB，**-62%**）；JS 656KB、CSS 113KB
- 构建耗时 **1.6~3.9s**（原先 8~17s）
- 已移除 `element-plus` 依赖，17 个路由全部使用自研 `ui/` 组件

**契约红线（7 条全部守住）**
公式渲染管线、流式两段式、错题 SRS、打卡/成就、账号双命名空间、打印约定、云端同步幂等 —— 语义一处未改。

---

## 三、已知遗留（不用报，已在计划里）

| 项 | 现状 | 何时解决 |
|---|---|---|
| 讲解非逐字流式 | 前端「渐进揭示」代替 | 后端补流式 explain 接口后，改 adapter |
| 相似题 / 变式题生成 | 演示数据（`lib/mock`） | 后端实现后改 adapter |
| AI 对话 | Mock 流式（回答开头写明"演示回答"） | 后端对话接口就绪后改 adapter |
| 课程 / 知识点 / 课表 | 本地存储，无云端同步 | 后端建表后接入同步 |
| 学习记录 | 本地存储（结构已对齐后端字段） | 同上 |
| 讲解深度 | 前端展示分层（不改变后端产出） | 后端支持 depth 参数后透传 |
| 社区 | 只有介绍页 | 未排期 |
| 10 个旧组件文件 | 已标 `[LEGACY-P8]`，不在构建图里 | 可随时删（删除操作在本机被安全机制拦） |

---

## 四、不通过时怎么告诉我

一行一条，格式：

```
序号 | 现象 | 复现步骤 | 期望
B3 | 点拍照没反应 | 手机 Chrome，拍题页点"拍照" | 应唤起相机
```

有截图更好。我会按这个列表逐条修，修完重新上线并再跑一遍自动点检。
