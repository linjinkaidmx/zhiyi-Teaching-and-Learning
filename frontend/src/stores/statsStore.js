/**
 * 转发模块（P2.5）
 * ---------------------------------------------------------------------------
 * 真正的实现仍在 src/statsStore.js（线上组件仍从那里引用）。
 * 这里只做转发，保证「无论从哪个路径引用，都是同一个单例」——
 * 若在此处放一份拷贝，会产生两套 state，属于严重 bug。
 * P3 清理组件引用后，把实现整体搬进本文件并删除根目录旧文件。
 */
export * from '../statsStore.js'
